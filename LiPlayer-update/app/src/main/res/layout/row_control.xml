<?xml version="1.0" encoding="utf-8"?>
<!-- Ряд меню. Панели раскрываются вправо от столбца, поэтому иконка слева,
     подпись за ней. Ряд фокусируемый: стрелки влево/вправо ловит
     OnKeyListener, вверх/вниз обрабатывает сам Android. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/btn_focus"
    android:focusable="true"
    android:gravity="center_vertical|left"
    android:orientation="horizontal"
    android:paddingLeft="8dp"
    android:paddingRight="12dp"
    android:paddingTop="7dp"
    android:paddingBottom="7dp">

    <ImageView
        android:id="@+id/row_icon"
        android:layout_width="26dp"
        android:layout_height="26dp" />

    <TextView
        android:id="@+id/row_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="12dp"
        android:textColor="#FFFFFF"
        android:textSize="17sp" />
</LinearLayout>
